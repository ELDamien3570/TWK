using UnityEngine;

namespace TWK.Economy
{
    public enum BuildingConstructionState
    {
        UnderConstruction,
        Completed,
        Paused // For future use (resource shortages, etc.)
    }
}
